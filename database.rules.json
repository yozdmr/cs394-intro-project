{
  "rules": {
    ".read": true,
    "admins": {
      ".read": "auth !== null"
    },
    "courses": {
      ".write": "auth !== null && root.child('admins').child(auth.uid).val() === true"
    }
  }
}
